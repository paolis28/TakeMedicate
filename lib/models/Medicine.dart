//El constructor debe llamarse igual que la clase, o ser un constructor con nombre.
// Lo más simple es que se llame igual que la clase.

class Medicine{
  final String nombre;
  final String horario;

  Medicine({required this.nombre, required this.horario});
}